<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ScoutSheet.PastMatches">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Export All" Clicked="ToolbarItem_Clicked_1"></ToolbarItem>
        <ToolbarItem Text="Clear Database" Clicked="ToolbarItem_Clicked"></ToolbarItem>
    </ContentPage.ToolbarItems>
    <ListView x:Name="MatchesListView" IsPullToRefreshEnabled="True" Refreshing="MatchesListView_Refreshing" ItemSelected="MatchesListView_ItemSelected">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding MatchNumberEntry}"></TextCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>